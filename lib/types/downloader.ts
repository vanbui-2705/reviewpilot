/**
 * Shared types for the downloader pipeline.
 * This file has ZERO imports so it can be safely imported
 * from anywhere without creating circular dependency chains.
 */

export type Platform = "youtube" | "tiktok" | "facebook" | "instagram";
